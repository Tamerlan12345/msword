# STYLUS Design System Log

## Guidelines
- **Consistency:** Use design tokens, not magic numbers.
- **Responsiveness:** Mobile First.
- **Accessibility:** Ensure high contrast, keyboard navigation, and semantic HTML.

## Log

### 2025-02-18 — [UX/A11y]
**Решение:**
- Заменил `div onClick` на `<Link>` и `<button>` в `Header.tsx`.
- Добавил `htmlFor` и `id` для полей ввода в `Login.tsx`.
- Оптимизировал отступы заголовка для мобильных устройств (`px-4 sm:px-6`).
- Добавил `aria-label` для иконок.

**Профит:**
- Полная поддержка навигации с клавиатуры (Tab/Enter).
- Поддержка скринридеров (Screen Readers) благодаря семантическим элементам и ARIA-атрибутам.
- Улучшенная эргономика форм (клик по лейблу ставит фокус в поле).
- Более компактный заголовок на узких экранах.

### 2025-02-18 — [UX/A11y]
**Решение:**
- В `Dashboard.tsx`: заменил карточки документов с `div onClick` на семантические `<Link>`.
- Добавил ARIA-роли (`tablist`, `tab`, `tabpanel`) для табов на главной странице.
- Добавил `focus-visible` состояния для карточек и табов.
- Добавил `aria-label` для карточек с указанием статуса документа.

**Профит:**
- Карточки документов теперь доступны с клавиатуры (Tab/Enter) и фокусируются.
- Скринридеры корректно объявляют статус документа в списке.
- Табы соответствуют паттерну ARIA, улучшая навигацию для пользователей вспомогательных технологий.

### 2026-02-25 — [UX/A11y]
**Решение:**
- В `NewDocumentModal.tsx`: Добавил `role="dialog"`, `aria-modal="true"` и `aria-labelledby`.
- Реализовал закрытие модального окна по клавише `Escape`.
- Добавил автофокус на поле названия при открытии.
- Оптимизировал отступы для мобильных устройств (`p-4` vs `p-6`).
- Стандартизировал цвета границ и добавил фокус-эффекты для области загрузки файла.

**Профит:**
- Полная доступность модального окна для скринридеров и клавиатурной навигации.
- Улучшенный UX: мгновенный ввод названия без лишних кликов.
- Корректное отображение на узких экранах (отступы не "съедают" контент).
- Визуальная чистота и консистентность с дизайн-токенами.

### 2026-02-25 — [UI/UX]
**Решение:**
- В `DocumentDetail.tsx`: Внедрил вертикальный таймлайн для отображения маршрута согласования.
- Заменил текстовое сообщение "Загрузка..." на анимированный спиннер `Loader2`.
- Добавил иконки статусов (CheckCircle, XCircle) и пульсирующий индикатор для текущего этапа.

**Профит:**
- Улучшена визуальная иерархия и читаемость прогресса документа.
- "Тактильный" отклик интерфейса за счет понятных статусных иконок.
- Устранение скачков контента при загрузке.

### 2026-02-25 — [UI/UX/A11y]
**Решение:**
- В `Profile.tsx`: Стандартизировал стили инпутов (единый дизайн с логином) и кнопок (добавил `Loader2`).
- Добавил `htmlFor` и `id` для всех полей ввода.
- Добавил `aria-live="polite"` для сообщений об ошибках/успехе.
- Внедрил микро-анимации нажатия (`active:scale-95`) для кнопок.

**Профит:**
- **Консистентность:** Страница профиля теперь выглядит как часть единой системы.
- **Доступность:** Скринридеры теперь корректно связывают лейблы с полями ввода.
- **UX:** Пользователь видит индикатор загрузки вместо «зависшей» кнопки.

### 2026-02-26 — [UI/UX/A11y]
**Решение:**
- В `Admin.tsx`: Полный рефакторинг страницы создания пользователя.
- Заменил стандартные алерты на стилизованные блоки уведомлений (`AlertCircle` / `CheckCircle`).
- Добавил иконку заголовка (`UserPlus`) и стандартизировал отступы.
- Внедрил состояние загрузки (`isLoading`) с блокировкой кнопки и спиннером `Loader2`.
- Применил глобальные стили для инпутов, лейблов и кнопок (focus-эффекты, active-состояния).

**Профит:**
- **Консистентность:** Административная панель теперь визуально соответствует `Login` и `Profile`.
- **Обратная связь:** Четкая индикация процесса создания и результата операции.
- **Доступность:** Полная поддержка скринридеров и клавиатурной навигации.
